<style>
    html, body { margin: 0; padding: 0; }
</style>
<script src="https://cdn.tailwindcss.com"></script>

<div class="w-full min-h-screen bg-fixed bg-center bg-cover flex flex-col items-center p-6 md:p-10" 
     style="background-image: url('https://images.unsplash.com/photo-1451187580459-43490279c0fa?q=80&w=2072&auto=format&fit=crop');">
    
    <div class="w-full max-w-5xl bg-black bg-opacity-60 backdrop-blur-xl border border-white border-opacity-20 rounded-3xl shadow-2xl p-8">
        
        <div class="mb-4 text-white font-bold">
            <?php echo $this->flash->output(); ?>
        </div>

        <div class="flex flex-col md:flex-row justify-between items-center mb-8 gap-4">
            <h1 class="text-3xl font-black text-white italic">🚀 Alumnos en Órbita</h1>
            <a href="<?php echo $this->url->get('hola/index'); ?>" class="bg-blue-600 hover:bg-blue-500 text-white font-bold py-2 px-6 rounded-full transition shadow-lg">+ Nuevo</a>
        </div>

        <div class="overflow-x-auto rounded-xl border border-white border-opacity-10">
            <table class="w-full text-left text-white border-collapse">
                <thead class="bg-white bg-opacity-10">
                    <tr>
                        <th class="p-4 text-blue-400 uppercase text-xs tracking-widest">ID</th>
                        <th class="p-4 uppercase text-xs tracking-widest">Nombre</th>
                        <th class="p-4 uppercase text-xs tracking-widest text-center">Registro (CDMX)</th>
                        <th class="p-4 uppercase text-xs tracking-widest text-right">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (isset($alumnos) && count($alumnos) > 0): ?>
                        <?php foreach ($alumnos as $alumno): ?>
                        <tr class="border-b border-white border-opacity-5 hover:bg-white hover:bg-opacity-5 transition">
                            <td class="p-4 font-mono text-blue-300">#<?php echo $alumno->id; ?></td>
                            <td class="p-4 font-bold"><?php echo $alumno->nombre; ?></td>
                            <td class="p-4 text-center text-sm opacity-80">
                                <?php 
                                    date_default_timezone_set('America/Mexico_City');
                                    echo date('d/m/Y - h:i A', strtotime($alumno->fecha_registro)); 
                                ?>
                            </td>
                            <td class="p-4 text-right">
                                <a href="<?php echo $this->url->get('hola/eliminar/' . $alumno->id); ?>" 
                                   onclick="return confirm('¿Estás segura de eliminar este registro?');"
                                   class="bg-red-600 hover:bg-red-500 text-white text-xs font-bold py-2 px-4 rounded-xl transition duration-300 shadow-md">
                                    🗑️ Eliminar
                                </a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                    <?php else: ?>
                        <tr>
                            <td colspan="4" class="p-10 text-center text-gray-400 italic">No hay tripulantes registrados todavía.</td>
                        </tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>