<style>
    html, body { margin: 0; padding: 0; }
</style>

<script src="https://cdn.tailwindcss.com"></script>

<div class="w-full min-h-screen bg-fixed bg-center bg-cover flex items-center justify-center px-4 sm:px-6 lg:px-10"
     style="background-image: url('https://images.unsplash.com/photo-1451187580459-43490279c0fa?q=80&w=2072&auto=format&fit=crop');">

    <div class="
        w-full 
        max-w-xl sm:max-w-3xl lg:max-w-5xl
        bg-black/60 backdrop-blur-xl
        border border-white/20
        rounded-3xl
        shadow-2xl
        p-5 sm:p-8 lg:p-10
    ">



    <nav class="w-full md:w-fit flex flex-wrap items-center text-base md:text-lg text-slate-400 mb-6 bg-slate-800/80 p-4 rounded-2xl border border-slate-700 shadow-lg backdrop-blur-sm">
    <a href="<?= $this->url->get('hola/index') ?>" class="hover:text-blue-400 transition flex items-center gap-2 group">
        <i data-lucide="home" class="w-5 h-5 group-hover:scale-110 transition-transform"></i> 
        <span>Inicio</span>
    </a>
    
    <span class="mx-3 text-slate-600">/</span>
    
    <span class="text-white font-bold flex items-center gap-2">
        <i data-lucide="table-2" class="w-5 h-5 text-sky-400"></i> 
        <span>Lista de Alumnos</span>
    </span>
</nav>


        <div class="mb-4 text-white font-bold text-sm sm:text-base">
            <?php echo $this->flash->output(); ?>
        </div>

        <div class="flex flex-col sm:flex-row justify-between items-center mb-8 gap-4">
            <h1 class="text-2xl sm:text-3xl font-black text-white italic text-center sm:text-left">
                🚀 Alumnos en Órbita
            </h1>
            <a href="<?php echo $this->url->get('hola/index'); ?>"
               class="bg-blue-600 hover:bg-blue-500 text-white font-bold py-2 px-6 rounded-full transition shadow-lg w-full sm:w-auto text-center">
                + Nuevo
            </a>
        </div>

        <div class="overflow-x-auto rounded-xl border border-white/10">
            <table class="min-w-full text-left text-white border-collapse text-sm sm:text-base">
                <thead class="bg-white/10">
                    <tr>
                        <th class="p-3 sm:p-4 text-blue-400 uppercase text-xs tracking-widest">ID</th>
                        <th class="p-3 sm:p-4 uppercase text-xs tracking-widest">Nombre</th>
                        <th class="p-3 sm:p-4 uppercase text-xs tracking-widest text-center hidden sm:table-cell">
                            Registro (CDMX)
                        </th>
                        <th class="p-3 sm:p-4 uppercase text-xs tracking-widest text-right">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (isset($alumnos) && count($alumnos) > 0): ?>
                        <?php foreach ($alumnos as $alumno): ?>
                        <tr class="border-b border-white/5 hover:bg-white/5 transition">
                            <td class="p-3 sm:p-4 font-mono text-blue-300">
                                #<?php echo $alumno->id; ?>
                            </td>
                            <td class="p-3 sm:p-4 font-bold break-words">
                                <?php echo $alumno->nombre; ?>
                            </td>
                            <td class="p-3 sm:p-4 text-center text-sm opacity-80 hidden sm:table-cell">
                                <?php 
                                    date_default_timezone_set('America/Mexico_City');
                                    echo date('d/m/Y - h:i A', strtotime($alumno->fecha_registro)); 
                                ?>
                            </td>
                            <td class="p-3 sm:p-4 text-right">
                                <a href="<?php echo $this->url->get('hola/eliminar/' . $alumno->id); ?>"
                                   onclick="return confirm('¿Estás segura de eliminar este registro?');"
                                   class="inline-block bg-red-600 hover:bg-red-500 text-white text-xs sm:text-sm font-bold py-2 px-4 rounded-xl transition shadow-md">
                                    🗑️ Eliminar
                                </a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                    <?php else: ?>
                        <tr>
                            <td colspan="4" class="p-8 sm:p-10 text-center text-gray-400 italic text-sm sm:text-base">
                                No hay tripulantes registrados todavía.
                            </td>
                        </tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>

    </div>
</div>

